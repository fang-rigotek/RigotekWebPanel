// src/authGate.tsx
// 登录门禁逻辑骨架（Step 8~10）

import type { Lang } from './i18n';

/** 启动门禁逻辑：后续负责自动登录/加载登录页/跳转 */
export async function startAuthGate(root: HTMLElement, lang: Lang): Promise<void> {
  // TODO: 实现 Step 8~10
  console.log('[AuthGate] start', { root, lang });
}
